<li>
	{{burger_name}}
	{{#if devoured}}
	<button class="change-devoured btn btn-outline-danger" data-id="{{id}}" data-newDevoured="{{devoured}}">
		Yum!
	</button>
	{{else}}
	<button class="delete btn btn-outline-danger" data-id="{{id}}" >
		X
	</button>
	{{/if}}
</li>